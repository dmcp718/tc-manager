# NPM Configuration for Production Builds

# Suppress deprecation warnings during install
loglevel=error

# Skip fund messages
fund=false

# Skip audit for production builds (handled separately)
audit=false

# Use exact versions for production stability
save-exact=true